# Legacy static documentation prototype

This directory contains the earlier standalone HTML documentation prototype.
It is retained as a design and content reference, but it is not deployed to
GitHub Pages.

The canonical documentation source is `docs/source`. GitHub Actions builds it
with Sphinx and `sphinx_rtd_theme`, then publishes the generated HTML from
`docs/build/html`.
